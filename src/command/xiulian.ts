// 修炼相关

import { Session } from "inspector";
import { Context } from "koishi";

export function handleXiulian(ctx: Context) {

  ctx.command("开始修炼").action(async ({ session }) => {
    // 查看当前




  });
}
